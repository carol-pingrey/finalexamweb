# TODO PAR PRIORITÉ et INSTRUCTIONS

1. base
    - tables [ok]
    - donnees de base [ok]
2. Enregistrer les donations
    - liste de villes (sans détails)
    - bouton par ville (recupère id ville et va vers la page de donation) (**$ville**)
    - formulaire pour donation (insert)
        - champs / input
            - Objet de don (à stocker dans **$obj_don**)
            - Quantité de don (à stocker dans **$qte_don**)
            - bouton "Faire un don" (ACTION : envoie données vers la base)
        * Action du bouton
            - INSERT dans la table DON
                - id_objets : **$obj_don**
                - quantite_dons : **$qte_don**
                - id_villes : **$ville**
3. afficher les details des besoins et donations des villes
    - sur le tableau de bord (bouclé pour toutes les villes)
        * JOIN ville + besoins
        * JOIN ville + donations
        - afficher besoins par ville
        - afficher donations par ville
    - sur la page donation (par récupération de l'id_ville)
        * JOIN ville + besoins
        * JOIN ville + donations
        - afficher besoins par ville
        - afficher dons par ville
        
